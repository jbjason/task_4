enum GenderEnum { male, female, others }

enum DocBookType { instant, online, chamber }

enum NavigateFrom { home, doctor }

enum NumberVerificationType { login, signup, accountdelete, completeSignUp }

enum ShowBottomSheetType { onlyLive, onlySchedule, all }

enum ScrollType { horizontal, vertical }

enum LoadingType { dashAppointment, appointment, patient, image, none }

enum AuthTypeEnum { signup, login, forget, reset, delete }

enum AppointmentStatus { instantLive, offer, schedule }
